import React from 'react';
import { View } from 'react-native';

const SeparatorV = props => {
    return (
        <View style={{ width: '100%', height: 1, backgroundColor: 'white' }}></View>
    );
};

export default SeparatorV;